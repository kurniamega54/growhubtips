// wrapper so that modules living under `src` can import the SEO helper the
// way the original instruction specified. The real implementation lives in
// ../../lib/seo-utils.ts (shared root folder).
export * from "@/lib/seo-utils";
